

```
- 确认每个仓库都已推送

- 转移主仓库

- 整理为一个仓库

- 写一个临时发布规程，为满足 github actions 打包测试

- github actions 打包测试

- 后端启动时打印内容修改，改为正确的文档地址

- 文档中官仓库地址修改、网地址修改、bsky链接修改
https://github.com/Tweblog/tweblog
https://tweblog.pages.dev
https://tweblog.sakiko.top
https://haruki.sakiko.top

- 文档 cf page 尝试部署

- 写一个发布规程，规范发布操作
改版本号、改文档

- 发布一个版本，完善x导入


```

本地测试electron打包
```
npm install -g yarn@1.22.22

export http_proxy=http://127.0.0.1:10809/
export https_proxy=http://127.0.0.1:10809/

yarn make
```

让三个前端，开发环境自动使用正确的base路径
```
tweet-blog-vue3
tweet-blog-public-vue3
tweblog-electron-vue3

let apiBaseUrl: string
let staticBaseUrl: string
// 开发环境
if (import.meta.env.DEV) {
  // console.log('当前是开发环境')
  apiBaseUrl = 'http://localhost:3000/api/'
  staticBaseUrl = 'http://localhost:3000/'
}
// 生产环境，将由后端来托管前端，设置为根路径
else {
  // console.log('当前是生产环境')
  apiBaseUrl = '/api/'
  staticBaseUrl = '/'
}
```


