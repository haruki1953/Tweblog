发布规程

## 版本更改
推荐用正则替换，即可直接替换前后端中的所有版本
```
"version": "0.0.0",
"version": "version",
```

前端部分
```
tweet-blog-vue3
tweet-blog-public-vue3
tweblog-electron-vue3
这三个目录是类似的，其中要在以下地方修改版本信息

package.json
src\config\info.ts
```

后端部分
```
tweet-blog-hono
tweblog-electron-hono
这两个目录是类似的，其中要在以下地方修改版本信息

package.json
src\configs\info.ts
```

tweblog-electron-hono 中还有一些配置要注意，其在测试时可能会修改，要保证其为以下
```ts
// src\configs\system.ts
export const systemDataPath = path.join(appElectron.getPath('documents'), 'Tweblog/data/')
// src\desktop\config.ts
export const getUrlIndexHtml = () => `http://127.0.0.1:${httpPort}/desktop/`
```

## 改文档、改changelog
改文档、改changelog，然后pr至主分支
```
README.md

CHANGELOG.md

Tweblog\src\guide\what-is-tweblog.md
中要改一下标题中的最新版本号Badge
要改一下文档中的当前版本

Tweblog\src\guide\changelog.md
尽量和changelog保持同步
```


## published release
release信息从changelog复制即可
